generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model Meeting {
  id         String   @id @default(cuid())
  title      String
  sourceType String
  rawText    String?
  createdAt  DateTime @default(now())
}